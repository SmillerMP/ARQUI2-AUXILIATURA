# Credenciales

- Usuario: pro-auxes
- Password: arqui2


# Enviroment

existe el archivo [](.env.example) que es una copia de como deberia de verse el .env original para que funcione docker compose, basta con renombrar el .example como .env

# Comandos Docker

- `docker compose build`
- `docker compose up`
- `docekr compose push`